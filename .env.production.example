APP_HOST=0.0.0.0
APP_PORT=8000
APP_TZ=Asia/Shanghai
MEMORY_DB_PATH=/app/data/memory.sqlite3

# 上线建议关闭公开注册，账号用 scripts/create_user.py 创建。
ALLOW_REGISTRATION=false

# 邮件提醒，可先留空。
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS=true
SMTP_SSL=false
REMINDER_POLL_SECONDS=60
