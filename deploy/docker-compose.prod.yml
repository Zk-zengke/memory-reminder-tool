services:
  memory-reminder:
    build: ..
    container_name: memory-reminder
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - ../.env.production
    volumes:
      - ../data:/app/data
